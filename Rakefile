#!/usr/bin/env ruby

# We use rake-compiler for this project.
require 'rake/extensiontask'

Rake::ExtensionTask.new do |ext|
  ext.name    = 'hash_rekey'
  ext.lib_dir = 'lib/carats'
end

#Rake::ExtensionTask.new('basicobject')
#Rake::ExtensionTask.new('string_tab')

